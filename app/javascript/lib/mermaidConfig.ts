import { MermaidConfig } from 'mermaid';

const mermaidConfig: MermaidConfig = {
  fontFamily: 'Telex, Arial, Helvetica, sans-serif',
  theme: 'base',
  themeVariables: {
    background: '#FFFFFF',
    fontSize: 20,
    primaryColor: '#FFFFFF',
    primaryBorderColor: '#000000',
  },
};

export default mermaidConfig;
